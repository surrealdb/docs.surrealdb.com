export default function SDKBoxes({ clientBoxes }) {
  return (
    <div className='row layout-boxes'>
      {clientBoxes.map((box, index) => (
        <div className='column' key={index}>
          <a href={box.link}>
            <div className='layout-boxes-item'>
              <div>
              <div className={box.available ? 'layout-boxes-container' : 'layout-boxes-container-fade'}>
                  <h4>
                    {box.icon}
                    <span>{box.label}</span>
                  </h4>
                  <div className='layout-boxes-available'>
                    <l className={box.available ? 'green' : 'grey'} r=''>
                      {box.available ? 'AVAILABLE' : 'COMING SOON'}
                    </l>
                  </div>
                </div>
              </div>
            </div>
          </a>
        </div>
      ))}
    </div>
  );
}

