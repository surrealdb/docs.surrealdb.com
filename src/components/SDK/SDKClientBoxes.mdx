export default function SDKBoxes({ clientBoxes }) {
  return (
    <div className='layout-boxes'>
      {clientBoxes.map((box, index) => (
        <div className='layout-boxes-content' key={index}>
          <a href={box.link}>
            <div className='layout-boxes-item'>
              <div>
                <h4>
                  {box.icon}
                  <span>{box.label}</span>
                </h4>
                <div>
                  <l
                    className={box.available ? 'green' : 'grey'}
                    r=''
                  >
                    {box.available ? 'AVAILABLE' : 'COMING SOON'}
                  </l>
                </div>
              </div>
            </div>
          </a>
        </div>
      ))}
    </div>
  );
}

