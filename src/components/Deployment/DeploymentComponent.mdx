export default function DeploymentBoxes({ deploymentBoxes }) {
  return (
    <div className='layout-boxes'>
      {deploymentBoxes.map((box, index) => (
        <div className='layout-boxes-content' key={index}>
          <a href={box.link}>
            <div className='layout-boxes-item'>
              <div>
                <div className='layout-boxes-container'>
                  <h4>
                    {box.icon}
                    <span>{box.label}</span>
                  </h4>
                  <div className='layout-boxes-available'>
                    <l className={box.available ? 'green' : 'grey'} r=''>
                      {box.available ? 'AVAILABLE' : 'COMING SOON'}
                    </l>
                  </div>
                </div>
                <div className='layout-boxes-copy'>
                  <p>{box.copy}</p>
                </div>
              </div>
            </div>
          </a>
        </div>
      ))}
    </div>
  );
}