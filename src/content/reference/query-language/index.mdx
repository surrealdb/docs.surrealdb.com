---
position: 1
title: SurrealQL
description: "SurrealQL statements, clauses, functions and language primitives. The full reference for the SurrealDB query language."
---

# SurrealQL

SurrealQL is SurrealDB's query language. Syntax is broadly SQL-like, with extensions for nested fields, graph edges, record IDs, and other SurrealDB-specific constructs.

For a guided introduction, see [Learn: Querying](/docs/learn/querying).

## Examples

A minimal query can follow a familiar SQL shape:

```surql
SELECT name,
       metadata
FROM   user
WHERE  age >= 18; 
```

Projections can include nested objects and graph traversals:

```surql
SELECT name,
       metadata.{
          date_registered,
          last_login
       },
       ->wrote->post AS posts
FROM user
WHERE age >= 18;
```

Further detail is organised under [Statements](/docs/reference/query-language/statements/overview) and the other sections in this reference.

## Clauses

- [Clauses](/docs/reference/query-language/clauses/overview) - the clauses statements share, such as `WHERE`, `LIMIT`, `START` and `OMIT`

## Language primitives

Reference pages for the pieces a query is built from:

- [Statements](/docs/reference/query-language/language-primitives/statements) - what a statement is, and how several combine into one query
- [Comments](/docs/reference/query-language/language-primitives/comments) - the three comment forms SurrealQL accepts
- [Data types](/docs/reference/query-language/language-primitives/data-types) - every value type, from records to geometries
- [Operators](/docs/reference/query-language/language-primitives/operators) - comparison, arithmetic, set and graph operators
- [Casting](/docs/reference/query-language/language-primitives/casting) - converting a value from one type to another
- [Idioms](/docs/reference/query-language/language-primitives/idioms) - path syntax for reaching into records and graphs
- [Parameters](/docs/reference/query-language/language-primitives/parameters) - built-in parameters and your own
- [Formatters](/docs/reference/query-language/language-primitives/formatters) - formatting datetimes and numbers
- [Record links](/docs/reference/query-language/language-primitives/record-links) - pointing at another record
- [Record references](/docs/reference/query-language/language-primitives/record-references) - links the database keeps in step for you

## Machine learning

- [ML functions](/docs/reference/query-language/functions/ml-functions/functions) - call a stored model from a query
