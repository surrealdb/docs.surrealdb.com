---
position: 5
title: Datetime
description: SurrealDB datetime values map to Java's ZonedDateTime class.
---

# Datetime {#datetime}

SurrealDB [`datetime`](/docs/reference/query-language/language-primitives/data-types/datetimes) values map to Java's `java.time.ZonedDateTime`. The SDK handles conversion automatically when deserializing query results into [`Value`](/docs/languages/java/api/values/value) objects or POJOs.

---

## Value methods

### `.isDateTime()` {#is-datetime}

Checks if the value is a datetime.

```java title="Method Syntax"
value.isDateTime()
```

**Returns:** `boolean`

### `.getDateTime()` {#get-datetime}

Returns the datetime as a `ZonedDateTime`.

```java title="Method Syntax"
value.getDateTime()
```

**Returns:** `java.time.ZonedDateTime`

---

## POJO mapping

When using typed methods, datetime fields in your POJO should be declared as `ZonedDateTime`.

```java
import java.time.ZonedDateTime;

public class Event {
    public RecordId id;
    public String title;
    public ZonedDateTime createdAt;

    public Event() {}
}

Optional<Event> event = db.select(Event.class, new RecordId("event", "conf"));
ZonedDateTime when = event.get().createdAt;
```

---

## Example

```java title="Working with datetime values"
import com.surrealdb.Surreal;
import com.surrealdb.Response;
import com.surrealdb.Value;
import com.surrealdb.signin.RootCredential;
import java.time.ZonedDateTime;

try (Surreal db = new Surreal()) {
    db.connect("ws://localhost:8000");
    db.useNs("surrealdb").useDb("docs");
    db.signin(new RootCredential("root", "root"));

    Response response = db.query("RETURN time::now()");
    Value result = response.take(0);

    if (result.isDateTime()) {
        ZonedDateTime now = result.getDateTime();
    }
}
```

---

## See also

- [Value types](/docs/languages/java/concepts/value-types) — Type mapping overview
- [Value](/docs/languages/java/api/values/value) — The Value class reference
- [SurrealQL datetimes](/docs/reference/query-language/language-primitives/data-types/datetimes) — Datetime types in SurrealDB
