---
position: 2
title: Installation
description: Learn how to install the stable v1 SurrealDB SDK for PHP into your project using Composer.
---

# Installation

Before you can use the v1 SDK in PHP, you need to install it via the [Composer](https://getcomposer.org/download/) package manager. This guide will walk you through the process of installing the SDK into your project.

> [!NOTE]
> This is the stable, currently released v1 SDK. It requires PHP version `8.2` or greater. For the next-generation alpha rewrite, see the [v2 alpha documentation](/docs/languages/php/v2/installation).

### Install the SDK

Within your Composer project, run the following command to install the SDK:

```bash
composer require surrealdb/surrealdb.php
```

### Using the SDK

Once you have installed the SDK, make sure to include the [Composer autoloader](https://getcomposer.org/doc/01-basic-usage.md#autoloading) in your PHP script:

```php
require __DIR__ . '/vendor/autoload.php';
```

You can now use the SDK in your PHP script by importing it:

```php
use Surreal\Surreal;
```

Now that you have the SDK installed and imported, you can start using the SDK to interact with SurrealDB.

## Next steps

- [Quickstart](/docs/languages/php/v1/start) for a complete working example
- [Initialization](/docs/languages/php/v1/concepts/initialization) to learn how to connect and select a namespace and database
- [Authentication](/docs/languages/php/v1/concepts/authentication) for signing in and managing credentials
