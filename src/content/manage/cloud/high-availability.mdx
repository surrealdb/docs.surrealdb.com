---
position: 4
title: High availability
description: "How fault tolerance and availability differ between SurrealDB Cloud Start and Scale plans."
---

# High availability

**High availability (HA)** means your database stays reachable and consistent when individual components fail or during planned maintenance. What you get on SurrealDB Cloud depends on your **plan** and instance topology.

## Start (single-node)

**Start** instances run SurrealDB on a **single compute node** with dedicated storage. This model is simple and cost-effective for development, staging, and workloads that tolerate brief downtime.

- There is **no multi-node failover** within the instance — if the node is unavailable, connections drop until the platform recovers it.
- **Pausing** an instance stops compute deliberately; **resuming** brings it back on the same configuration.
- Protect production data with **[automated backups](/docs/manage/cloud/backups-and-recovery)** and tested restore procedures rather than assuming in-instance redundancy.

For workloads that require continuous uptime across hardware or zone failures, use the **Scale** plan or architect retries and failover at the application layer.

## Scale (multi-node on SurrealDS)

**Scale** clusters run multiple SurrealDB query nodes against [SurrealDS](https://surrealdb.com/platform/surrealds) distributed storage. HA is built into the architecture:

- **Multiple compute nodes** — you configure **compute units** (minimum three on Scale) so query capacity is spread across nodes.
- **Replication and quorum consensus** — SurrealDS coordinates writes and tolerates node loss without splitting the dataset across independent single-node databases.
- **Compute can be resized or replaced** — durable state lives in the storage tier, not on individual query nodes.

Scale is the managed option when you need fault-tolerant, horizontally scalable production without operating your own Kubernetes cluster. See [Cloud architecture](/docs/build/deployment/surrealdb-cloud/architecture) and [Scaling](/docs/manage/cloud/scaling).

## Choosing between Start and Scale for HA

| Requirement | Start | Scale |
| --- | --- | --- |
| Accept brief maintenance or node recovery windows | Suitable | Suitable |
| Survive node failure without manual failover | Limited | Built-in (multi-node + SurrealDS) |
| Horizontal query scale | Resize vertically only | Add compute nodes |
| Operational burden | Lowest | Managed cluster |

## Related topics

- [Instance management](/docs/manage/cloud/instance-management) — pause, resume, and delete instances.
- [Backups & recovery](/docs/manage/cloud/backups-and-recovery) — recover from logical mistakes or regional issues.
- [Patches & upgrades](/docs/manage/cloud/patches-and-upgrades) — change SurrealDB version with platform-managed rollouts.
