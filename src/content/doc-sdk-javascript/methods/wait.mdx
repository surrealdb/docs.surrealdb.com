---
sidebar_position: 1
sidebar_label: wait
title: JavaScript | SDK | Methods | wait
description: The SurrealDB SDK for JavaScript enables you to wait for a specific condition to be met before proceeding.
---

# `.wait()` {#wait}

Waits for the connection to the database to succeed.

```ts
await db.wait();
```

### Example usage

```ts
await db.wait();
```