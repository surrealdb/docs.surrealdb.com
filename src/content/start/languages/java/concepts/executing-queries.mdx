---
position: 3
title: Executing queries
description: The Java SDK provides methods for executing SurrealQL queries with optional parameter binding.
---

# Executing queries

The Java SDK provides methods for executing raw [SurrealQL](/docs/reference/query-language) queries against SurrealDB. You can run single or multi-statement queries, bind [parameters](/docs/reference/query-language/language-primitives/parameters) for safe variable injection, and call server-side [functions](/docs/reference/query-language/statements/define/function).

## API References

<table>
	<thead>
		<tr>
			<th scope="col">Method</th>
			<th scope="col">Description</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td scope="row" data-label="Method"><a href="/docs/start/languages/java/api/core/surreal#query"><code>db.query(sql)</code></a></td>
			<td scope="row" data-label="Description">Executes a SurrealQL query</td>
		</tr>
		<tr>
			<td scope="row" data-label="Method"><a href="/docs/start/languages/java/api/core/surreal#query-bind"><code>db.queryBind(sql, params)</code></a></td>
			<td scope="row" data-label="Description">Executes a parameterized query</td>
		</tr>
		<tr>
			<td scope="row" data-label="Method"><a href="/docs/start/languages/java/api/core/surreal#run"><code>db.run(name, args)</code></a></td>
			<td scope="row" data-label="Description">Runs a SurrealDB function</td>
		</tr>
	</tbody>
</table>

## Running a query

The [`.query()`](/docs/start/languages/java/api/core/surreal#query) method executes one or more SurrealQL statements and returns a [`Response`](/docs/start/languages/java/api/core/response). Use [`.take(int)`](/docs/start/languages/java/api/core/response#take) to extract the result of a specific statement by its zero-based index, or `.take(Class, int)` to deserialize the result into a typed Java object.

```java
Response response = db.query("SELECT * FROM users; SELECT * FROM posts;");

Value users = response.take(0);
List<Post> posts = response.take(Post.class, 1);
```

## Using query parameters

The [`.queryBind()`](/docs/start/languages/java/api/core/surreal#query-bind) method accepts a `Map<String, ?>` of parameters that are safely injected into the query. Parameterized queries prevent SurrealQL injection and ensure values are properly escaped.

```java
Map<String, Object> params = Map.of("min_age", 18);

Response response = db.queryBind(
    "SELECT * FROM users WHERE age > $min_age",
    params
);

List<User> users = response.take(User.class, 0);
```

Always prefer [`.queryBind()`](/docs/start/languages/java/api/core/surreal#query-bind) over string concatenation when incorporating user-provided values into queries.

## Working with query responses

The [`Response`](/docs/start/languages/java/api/core/response) object contains the results of all statements in the query. Use [`.take(int)`](/docs/start/languages/java/api/core/response#take) to get a raw [`Value`](/docs/start/languages/java/api/values/value), or `.take(Class, int)` to deserialize into a POJO. The `.size()` method returns the number of statement results.

```java
Response response = db.query(
    "CREATE users SET name = 'Alice'; SELECT * FROM users;"
);

int statementCount = response.size();

Value created = response.take(0);
List<User> users = response.take(User.class, 1);
```

See [Value types](/docs/start/languages/java/concepts/value-types) for details on working with the [`Value`](/docs/start/languages/java/api/values/value) class and type conversions.

## Running SurrealDB functions

The [`.run()`](/docs/start/languages/java/api/core/surreal#run) method calls a server-side function defined with [`DEFINE FUNCTION`](/docs/reference/query-language/statements/define/function). Pass the function name and any arguments.

```java
Value result = db.run("fn::calculate_total", 100, 0.2);
```

## Learn more

- [Surreal API reference](/docs/start/languages/java/api/core/surreal) for method signatures
- [Response API reference](/docs/start/languages/java/api/core/response) for response handling
- [Value types](/docs/start/languages/java/concepts/value-types) for working with query results
- [SurrealQL](/docs/reference/query-language) for query language reference
- [SurrealQL parameters](/docs/reference/query-language/language-primitives/parameters) for parameter syntax in queries
- [DEFINE FUNCTION](/docs/reference/query-language/statements/define/function) for defining server-side functions
