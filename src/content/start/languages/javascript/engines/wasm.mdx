---
position: 2
title: WebAssembly
description: The SurrealDB SDK for JavaScript using the WebAssembly engine.
---

# WebAssembly engine

The `@surrealdb/wasm` package is a plugin for the [JavaScript SDK](/docs/start/languages/javascript/installation) that runs SurrealDB as an embedded database within a browser environment. It supports in-memory databases and persistent storage via IndexedDB, and can optionally run inside a Web Worker.

> [!IMPORTANT]
> This package works with ES modules (`import`), not CommonJS (`require`).

## Installation

First, [install the JavaScript SDK](/docs/start/languages/javascript/installation) if you haven't already. Then add the WASM engine:

<Tabs groupId="package-manager">
<TabItem value="npm" label="npm" default>

```bash
npm install --save @surrealdb/wasm
```

</TabItem>
<TabItem value="yarn" label="yarn">

```bash
yarn add @surrealdb/wasm
```

</TabItem>
<TabItem value="pnpm" label="pnpm">

```bash
pnpm install @surrealdb/wasm
```

</TabItem>
</Tabs>

## Quick start

```ts
import { Surreal, createRemoteEngines } from 'surrealdb';
import { createWasmEngines } from '@surrealdb/wasm';

const db = new Surreal({
    engines: {
        ...createRemoteEngines(),
        ...createWasmEngines(),
    },
});

await db.connect('mem://');
```

For a complete guide covering engine registration, supported protocols (`mem://`, `indxdb://`), Web Worker support, and bundler configuration, see the [WebAssembly concept page](/docs/start/languages/javascript/concepts/web-assembly).

## Learn more

- [WebAssembly concept page](/docs/start/languages/javascript/concepts/web-assembly) for detailed setup, Web Worker support, and bundler configuration
- [Connecting to SurrealDB](/docs/start/languages/javascript/concepts/connecting-to-surrealdb) for connection options and reconnection behavior
- [`@surrealdb/wasm` on npm](https://npmjs.com/package/@surrealdb/wasm)
