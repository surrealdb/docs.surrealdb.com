---
sidebar_position: 1
sidebar_label: close
title: Close Method in PHP | PHP SDK | Integration | SurrealDB
description: The ->close() method for the SurrealDB SDK for PHP closes the persistent connection to the database.
---

# `->close()` {#close}

Closes the persistent connection to the database.

```php title="Method Syntax"
$db->close()
```

### Example usage
```php
$db->close();
```