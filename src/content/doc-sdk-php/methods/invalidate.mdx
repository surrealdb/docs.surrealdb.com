---
sidebar_position: 1
sidebar_label: invalidate
title: Invalidate Method in PHP | PHP SDK | Integration | SurrealDB
description: The ->invalidate() method for the SurrealDB SDK for PHP invalidates the authentication for the current connection.
---

# `->invalidate()` {#invalidate}

Invalidates the authentication for the current connection.

```php title="Method Syntax"
$db->invalidate()
```

### Example usage
```php
$db->invalidate();
```