---
position: 5
title: JavaScript SDK reference
description: "Package layout and configuration for @surrealdb/spectron."
---

# JavaScript SDK reference

| Item | Value |
| --- | --- |
| npm package | `@surrealdb/spectron` |
| Source | `spectron/clients/typescript/` |
| OpenAPI input | `doc/spec/*.swagger.json` |

## Install

```bash
npm install @surrealdb/spectron
```

## Configuration

```typescript
import { createSpectronClient } from "@surrealdb/spectron";

const client = createSpectronClient({
  baseUrl: process.env.SPECTRON_URL!,
  apiKey: process.env.SPECTRON_API_KEY!,
  contextId: process.env.SPECTRON_CONTEXT_ID!,
});
```

Uses **`Authorization: Bearer`** header authentication.

## Core operations

Same REST mapping as Python; see the [REST API](/docs/spectron/reference/rest-api).

## Harness adapter

```bash
npm install @surrealdb/spectron-vercel-ai
```

→ [Vercel AI SDK](/docs/spectron/integrations/frameworks/vercel-ai-sdk)

## User guide

→ [JavaScript and TypeScript SDK](/docs/spectron/integrations/sdks/javascript-and-typescript)
