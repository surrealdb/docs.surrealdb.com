---
sidebar_position: 1
sidebar_label: Setup
title: PHP | SDKs | Integration
description: The SurrealDB SDK for PHP enables simple and advanced querying of a remote or embedded database.
---

import ThemedImage from '@theme/ThemedImage';
import useBaseUrl from '@docusaurus/useBaseUrl';

<ThemedImage
  alt="`PHP`"
  width="50"
  className="sdk-logo"
  sources={{
    light: useBaseUrl('img/light/php.png'),
    dark: useBaseUrl('img/php-icon.png'),
  }}
/>

# PHP SDK

### Install the SDK

First, install the SurrealDB SDK using composer:

```bash
composer require surrealdb/surrealdb [version]
```
Version can be omitted if you do not want to use a specific version.