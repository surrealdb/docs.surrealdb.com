---
sidebar_position: 13
---

import TableComponent from '../../../src/components/TableData/TableComponent';
import { data } from '../../../src/components/TableData/SurrealQL/Datamodel/Casting/TableData';

# Casting 

## Casting

In the SurrealDB type system, values can be converted to other values efficiently. This is useful if input is specified in a query which must be of a certain type, or if a user has provided a parameter with an incorrect type.


<table>
<thead>
  <tr>
    <th>Type</th>
    <th>Description</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td><a href="/docs/surrealql/datamodel/casting#bool">&lt;bool&gt;</a></td>
    <td>Casts the subsequent value into a boolean</td>
  </tr>
  <tr>
    <td><a href="/docs/surrealql/datamodel/casting#int">&lt;int&gt;</a></td>
    <td>Casts the subsequent value into a int</td>
  </tr>
  <tr>
    <td><a href="/docs/surrealql/datamodel/casting#float">&lt;float&gt;</a></td>
    <td>Casts the subsequent value into a float</td>
  </tr>
  <tr>
    <td><a href="/docs/surrealql/datamodel/casting#string">&lt;string&gt;</a></td>
    <td>Casts the subsequent value into a string</td>
  </tr>
  <tr>
    <td><a href="/docs/surrealql/datamodel/casting#number">&lt;number&gt;</a></td>
    <td>Casts the subsequent value into a decimal</td>
  </tr>
  <tr>
    <td><a href="/docs/surrealql/datamodel/casting#decimal">&lt;decimal&gt;</a></td>
    <td>Casts the subsequent value into a decimal</td>
  </tr>
  <tr>
    <td><a href="/docs/surrealql/datamodel/casting#datetime">&lt;datetime&gt;</a></td>
    <td>Casts the subsequent value into a datetime</td>
  </tr>
  <tr>
    <td><a href="/docs/surrealql/datamodel/casting#duration">&lt;duration&gt;</a></td>
    <td>Casts the subsequent value into a duration</td>
  </tr>
</tbody>
</table>

<div className='layout-group'>

<div className='heading'>
    <div id="bool" className='code'>
    <code> &lt;bool&gt; </code>
    </div>
</div>

The &lt;bool&gt; casting function converts a value into a boolean.

```surql
SELECT * FROM <bool> "true";

true
```

```surql
SELECT * FROM <bool> "some truthy value";

true
```

```surql
SELECT * FROM <bool> 1.375;

true
```
```surql
SELECT * FROM <bool> 1.375;

false
```
</div>

<div className='layout-group'>

<div className='heading'>
    <div id="int" className='code'>
    <code> &lt;int&gt; </code>
    </div>
</div>

The &lt;int&gt; casting function converts a value into an integer.



```surql
SELECT * FROM <int> 13.572948467293847293841093845679289;

13
```
```surql
SELECT * FROM <int> "13.572948467293847293841093845679289";

13 
```

```surql 
SELECT * FROM <int> "some truthy value";

0
```

```surql
SELECT * FROM <int> true;

1
```

```surql
SELECT * FROM <int> false;

0

```
</div>

<div className='layout-group'>

<div className='heading'>
    <div id="float" className='code'>
    <code> &lt;float&gt; </code>
    </div>
</div>

The &lt;float&gt; casting function converts a value into a floating point number.

```surql
SELECT * FROM <float> 13.572948467293847293841093845679289;

13.572948467293847
```
```surql
SELECT * FROM <float> "13.572948467293847293841093845679289";

13.572948467293847
```

```surql
SELECT * FROM <float> "some truthy value";

0.0
```

```surql
SELECT * FROM <float> true;

1.0
```

```surql
SELECT * FROM <float> false;

0.0
```
</div>

<div className='layout-group'>

<div className='heading'>
    <div id="string" className='code'>
    <code> &lt;string&gt; </code>
    </div>
</div>

The &lt;string&gt; casting function converts a value into a string.


```surql
SELECT * FROM <string> true;

"true"
```
```surql
SELECT * FROM <string> 1.3463;

"1.3463"
```
```surql
SELECT * FROM <string> false;

"false"
```
</div>

<div className='layout-group'>

<div className='heading'>
    <div id="number" className='code'>
    <code> &lt;number&gt; </code>
    </div>
</div>

The &lt;number&gt; casting function converts a value into an infinite precision decimal number.

```surql
SELECT * FROM <number> 13.572948467293847293841093845679289;

"13.572948467293847293841093845679289"
```
```surql
SELECT * FROM <number> "13.572948467293847293841093845679289";

"13.572948467293847293841093845679289"
```
```surql
SELECT * FROM <number> "some truthy value";

"0"
```
```surql
SELECT * FROM <number> 1.193847193847193847193487E11;

"119384719384.7193847193487"
```
```surql
SELECT * FROM <number> true;

"1"
```

</div>

<div className='layout-group'>

<div className='heading'>
    <div id="decimal" className='code'>
    <code> &lt;decimal&gt; </code>
    </div>
</div>

The &lt;decimal&gt; casting function converts a value into an infinite precision decimal number.

```surql
SELECT * FROM <decimal> 13.572948467293847293841093845679289;

"13.572948467293847293841093845679289"
```
```surql
SELECT * FROM <decimal> "13.572948467293847293841093845679289";

"13.572948467293847293841093845679289"
```
```surql
SELECT * FROM <decimal> "some truthy value";

"0"
```
```surql
SELECT * FROM <decimal> 1.193847193847193847193487E11;

"119384719384.7193847193487"
```
```surql
SELECT * FROM <decimal> true;

"1"
```

</div>

<div className='layout-group'>

<div className='heading'>
    <div id="decimal" className='code'>
    <code> &lt;decimal&gt; </code>
    </div>
</div>

The &lt;decimal&gt; casting function converts a value into an infinite precision decimal number.

```surql
SELECT * FROM <decimal> 13.572948467293847293841093845679289;

"13.572948467293847293841093845679289"
```
```surql
SELECT * FROM <decimal> "13.572948467293847293841093845679289";

"13.572948467293847293841093845679289"
```
```surql
SELECT * FROM <decimal> "some truthy value";

"0"
```
```surql
SELECT * FROM <decimal> 1.193847193847193847193487E11;

"119384719384.7193847193487"
```
```surql
SELECT * FROM <decimal> true;

"1"
```

</div>



<div className='layout-group'>

<div className='heading'>
    <div id="datetime" className='code'>
    <code> &lt;datetime&gt; </code>
    </div>
</div>

The &lt;datetime&gt; casting function converts a value into a datetime.

```surql
SELECT * FROM <datetime> "2022-06-07 will be parsed";

"2022-06-07"
```
</div>

<div className='layout-group'>

<div className='heading'>
    <div id="duration" className='code'>
    <code> &lt;duration&gt; </code>
    </div>
</div>

The &lt;duration&gt; casting function converts a value into a duration.

```surql
SELECT * FROM <duration> "1h30m will be parsed";

"1h30m"
```
</div>